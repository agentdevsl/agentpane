# Findings Matrix

**Architecture Review Date:** 2026-02-17
**Documents Reviewed:** 10 (01-Project Structure through 10-Cross-Cutting Concerns)

---

## Summary Statistics

| Severity | Count |
|----------|-------|
| Critical | 0 |
| High | 12 |
| Medium | 42 |
| Low | 50 |
| Info | 4 |
| **Total** | **108** |

### Breakdown by Review Area

| Area | Critical | High | Medium | Low | Info | Total |
|------|----------|------|--------|-----|------|-------|
| 01 - Project Structure | 0 | 2 | 7 | 6 | 0 | 15 |
| 02 - Backend API & Services | 0 | 1 | 7 | 5 | 0 | 13 |
| 03 - Frontend Architecture | 0 | 1 | 4 | 5 | 3 | 13 |
| 04 - Agent Execution | 0 | 3 | 5 | 6 | 0 | 14 |
| 05 - Container & Sandbox | 0 | 0 | 5 | 11 | 0 | 16 |
| 06 - Database & Data Layer | 0 | 3 | 5 | 5 | 0 | 13 |
| 07 - Testing Infrastructure | 0 | 2 | 4 | 5 | 0 | 11 |
| 08 - Prompts, Templates & Workflows | 0 | 0 | 5 | 7 | 0 | 12 |
| 09 - CLI Monitor | 0 | 0 | 5 | 10 | 1 | 16 |
| 10 - Cross-Cutting Concerns | 0 | 1 | 4 | 5 | 0 | 10 |

---

## Consolidated Findings

| # | ID | Area | Finding | Severity | File Path | Recommendation |
|---|-----|------|---------|----------|-----------|----------------|
| 1 | PS-003 | Project Structure | Missing formal workspace configuration -- three independent packages with no `"workspaces"` in root `package.json`, causing inconsistent installs and no dependency sharing | High | `package.json:1-120`, `agent-runner/package.json:1-26`, `docker/Dockerfile:18` | Add `"workspaces": ["packages/*", "agent-runner"]` to root `package.json`; consolidate to single lock file |
| 2 | PS-006 | Project Structure | No build verification in CI -- pipeline runs lint/typecheck/tests but never runs `bun run build`, no Docker build, no E2E tests, no package build verification | High | `.github/workflows/ci.yml:1-75` | Add a `build` job that runs the full build pipeline; add Docker build verification and smoke test |
| 3 | BA-002 | Backend API & Services | Authentication middleware ineffective -- dev mode always succeeds with `local-dev` user; even with tokens, no database validation is performed; tokens accepted at face value | High | `src/lib/api/auth-middleware.ts:108-135`, `src/server/router.ts:81-97` | Pass real validators to `getAuthContext()`; validate Bearer tokens against `api_keys` table; require explicit `SKIP_AUTH=true` for dev bypass |
| 4 | FE-001 | Frontend Architecture | Hardcoded API base URL `http://localhost:3001` prevents deployment to any environment without code changes | High | `src/lib/api/client.ts:12` | Read from `import.meta.env.VITE_API_URL` or derive from `window.location.origin`; provide default for local dev |
| 5 | AE-001 | Agent Execution | AbortController created but never passed to SDK session -- `stop()` calls `controller.abort()` but signal never reaches the Claude Agent SDK, so agents continue running despite being "stopped" | High | `src/services/agent/agent-execution.service.ts:171-172,455`, `src/lib/agents/stream-handler.ts:122-128` | Pass `AbortController.signal` to both planning and execution functions; wire into SDK session or use to break stream loop |
| 6 | AE-002 | Agent Execution | Race condition between task move and concurrency check -- task moved to `in_progress` before worktree/session created; concurrent `start()` calls can both pass concurrency check (TOCTOU) | High | `src/services/agent/agent-execution.service.ts:96-106,108-127` | Wrap start sequence in DB transaction; move task only after all resources created; use optimistic locking or row lock |
| 7 | AE-009 | Agent Execution | `resume()` does not actually resume agent execution -- sets status to `running` but does not restart SDK session; no execution continuation path after planning for host-mode agents | High | `src/services/agent/agent-execution.service.ts:494-522`, `src/server/routes/tasks.ts:259-281` | Create new SDK session via `runAgentExecution` in `resume()`; add host-mode execution path in `approvePlan` |
| 8 | DB-001 | Database & Data Layer | Dual schema maintenance creates drift -- SQLite and PostgreSQL schemas maintained as separate file trees; several columns already drifted (`sandbox_provider`, `sandbox_container_id`, session summary metrics) | High | `src/db/schema/sqlite/sessions.ts:22-23`, `src/db/schema/postgres/sessions.ts:1-27` | Generate PG schema from single source-of-truth, or drop PG support if untested; add CI check comparing column sets |
| 9 | DB-002 | Database & Data Layer | Ad-hoc migration strategy lacks versioning -- fragmented across Drizzle Kit, hand-written SQL files, and embedded SQL constants in bootstrap; no rollback capability; no version tracking | High | `src/lib/bootstrap/phases/schema.ts:7-427`, `src/db/migrations/meta/_journal.json:1-13` | Consolidate on single migration strategy with versioned migration runner tracking applied migrations |
| 10 | DB-003 | Database & Data Layer | Missing transactions for multi-step operations -- `moveColumn` creates session then updates task without transaction; only `SettingsService.setMany()` uses transactions | High | `src/services/task.service.ts:321-410,584-643` | Wrap multi-step DB operations in `db.transaction()`; use outbox pattern for operations mixing DB writes with side effects |
| 11 | TI-002 | Testing Infrastructure | Frontend code entirely excluded from coverage -- `src/app/` excluded from coverage config, creating blind spot for entire React UI layer | High | `vitest.config.ts:40-41` | Remove `src/app/` exclusion or create separate coverage profile for frontend code |
| 12 | TI-005 | Testing Infrastructure | No tests for critical agent execution path -- `stream-handler.ts` and `agent-execution.service.ts` (most complex business logic) have no dedicated test coverage | High | `src/lib/agents/stream-handler.ts`, `src/services/agent/agent-execution.service.ts` | Create integration tests using existing `createPlanningStream()`/`createExecutionStream()` helpers |
| 13 | CC-001 | Cross-Cutting Concerns | 720 raw `console.*` calls across 89 files vs. only 25 structured logger usages -- majority of logging bypasses structured logger, losing request ID correlation and log-level filtering | High | `src/server/api.ts` (34 calls), `src/services/task-creation.service.ts` (117 lines), 87 more files | Adopt `no-console` lint rule; migrate high-traffic files to `createLogger()` first |
| 14 | PS-001 | Project Structure | Stale build artefacts committed -- timestamped JS files in repo root are auto-generated config transpilations with embedded timestamps | Medium | `app.config.timestamp_1768697232387.js`, `vite.config.timestamp_1768698877204.js` | Add `*.timestamp_*.js` to `.gitignore` and remove from repository |
| 15 | PS-004 | Project Structure | Package name mismatch -- root `package.json` still uses `"name": "agent-dev-template"` from original template | Medium | `package.json:2-4` | Update name to `agentpane` or `@agentpane/app` |
| 16 | PS-005 | Project Structure | Node.js engine version inconsistency across packages -- root requires `>=24.0.0`, sub-packages accept `>=22.0.0`, Docker uses Bun | Medium | `package.json:7`, `agent-runner/package.json:25`, `packages/cli-monitor/package.json:41` | Align all packages to same minimum version; consider specifying Bun |
| 17 | PS-008 | Project Structure | Dual lock files -- both `package-lock.json` (444KB) and `bun.lock` (248KB) at root; agent-runner also has both | Medium | `package-lock.json`, `bun.lock`, `agent-runner/package-lock.json`, `agent-runner/bun.lock` | Remove `package-lock.json`; add to `.gitignore`; use `bun install --frozen-lockfile` in CI/Docker |
| 18 | PS-010 | Project Structure | Monolithic `src/server/api.ts` at 1,418 lines handles app init, middleware, route mounting, and server startup | Medium | `src/server/api.ts:1-1418` | Extract middleware config, route registration, and server bootstrap into separate modules |
| 19 | PS-011 | Project Structure | Oversized service files -- `task-creation.service.ts` (2,544 lines) and `container-agent.service.ts` (2,244 lines) | Medium | `src/services/task-creation.service.ts`, `src/services/container-agent.service.ts` | Decompose into focused sub-modules (validation, AI interaction, persistence layers) |
| 20 | PS-013 | Project Structure | Missing `.env.example` -- no documentation of required/optional environment variables; devs must read multiple files | Medium | (project root) | Create `.env.example` documenting all env vars with descriptions, defaults, and required/optional status |
| 21 | PS-015 | Project Structure | Pre-commit hook runs full test suite on every commit, making commits take 30+ seconds | Medium | `.pre-commit-config.yaml:24-35` | Move test suite to pre-push hook; run only affected tests or quick smoke test on pre-commit |
| 22 | BA-001 | Backend API & Services | Monolithic API entry point -- 1,419-line file with DB init, migration, 20+ service instantiations, sandbox init (504 lines), and shutdown | Medium | `src/server/api.ts:1-1419` | Extract into `db-init.ts`, `service-registry.ts`, `sandbox-init.ts`, `shutdown.ts` |
| 23 | BA-003 | Backend API & Services | N+1 query pattern in `/api/projects/summaries` -- 70 queries for 10 projects with 5 running agents; duplicated in route handler and service | Medium | `src/server/routes/projects.ts:147-251`, `src/services/project.service.ts:183-255` | Replace with single aggregation query using `GROUP BY` and `JOIN`; consolidate into service |
| 24 | BA-004 | Backend API & Services | Temporal coupling in service initialization -- `TaskService` created with stub then patched via `setWorktreeService()` | Medium | `src/server/api.ts:346-360,535-540,1154` | Use lazy initialization or service container that resolves dependencies at first use |
| 25 | BA-006 | Backend API & Services | Duplicate task state transition definitions with conflicting rules -- strict in error catalog, permissive in `task-transitions.ts` | Medium | `src/lib/errors/task-errors.ts:5-9`, `src/services/task-transitions.ts:13-18` | Remove transition map from error file; enforce one canonical definition consistently |
| 26 | BA-007 | Backend API & Services | Route handlers bypass service layer -- direct DB queries and shell commands in route handlers instead of delegating to services | Medium | `src/server/routes/projects.ts:1-489`, `src/server/routes/settings.ts:1-127`, `src/server/routes/git.ts:1-453` | Move all DB access and shell commands to service layer; route handlers should only orchestrate |
| 27 | BA-009 | Backend API & Services | SSE connection management complexity -- 200+ lines of stream setup, replay, subscription, and cleanup in route handler | Medium | `src/server/routes/sessions.ts:117,352-559` | Extract into dedicated `SSEStreamManager` class for testability |
| 28 | BA-013 | Backend API & Services | Missing transaction boundaries -- multi-step DB operations without transactions; project deletion leaves orphaned data on partial failure | Medium | `src/services/task.service.ts:321-410`, `src/server/routes/projects.ts:417-424` | Wrap in `db.transaction()`; at minimum project deletion and task column moves |
| 29 | BA-014 | Backend API & Services | Shell command injection mitigations are partial -- string interpolation into `sh -c` commands despite validation; `limit` interpolated without shell escaping | Medium | `src/server/routes/git.ts:171-173,272-273`, `src/server/shared.ts:40-49` | Use array-based command execution (`Bun.spawn`) instead of shell string interpolation |
| 30 | FE-002 | Frontend Architecture | Deprecated `navigator.platform` usage for platform detection in two files | Medium | `src/app/hooks/use-keyboard-shortcuts.ts:63`, `src/app/components/features/project-picker/search-input.tsx:18` | Replace with `navigator.userAgentData?.platform` with fallback |
| 31 | FE-003 | Frontend Architecture | `window.confirm()` used in 7 locations for destructive actions, breaking visual design | Medium | `src/app/routes/templates/project.tsx:179`, `src/app/routes/templates/org.tsx:107`, 5 more files | Replace with reusable confirmation dialog using existing `Dialog` primitive |
| 32 | FE-004 | Frontend Architecture | ThemeToggle does not listen for system theme changes when "System" mode selected | Medium | `src/app/components/features/theme-toggle.tsx:35-47` | Add `matchMedia('prefers-color-scheme: dark')` change listener |
| 33 | FE-005 | Frontend Architecture | ThemeToggle custom dropdown lacks accessibility -- no ARIA attributes, focus trapping, or keyboard navigation | Medium | `src/app/components/features/theme-toggle.tsx:56-98` | Replace with existing Radix `DropdownMenu` primitive |
| 34 | FE-006 | Frontend Architecture | Dashboard data fetching waterfall -- 4 independent API calls fired sequentially in `useEffect` | Medium | `src/app/routes/index.tsx:150-207` | Use `Promise.all` or TanStack Router's `loader` for parallel prefetching |
| 35 | AE-003 | Agent Execution | Recovery system defines retry logic but never retries -- `withRetry()` defined but never called; `shouldRetry` flag ignored | Medium | `src/lib/agents/recovery.ts:34-62,79-128`, `src/services/agent/agent-execution.service.ts:404-443` | Implement actual retry logic or remove retry code to avoid false confidence |
| 36 | AE-004 | Agent Execution | `planning` status not in spec state machine; causes DB enum mismatch between agents and agentRuns tables | Medium | `src/services/agent/agent-execution.service.ts:175,293-315`, `src/lib/agents/stream-handler.ts:34` | Add `planning` to formal state machine spec and DB enum, or use existing state with separate `phase` field |
| 37 | AE-005 | Agent Execution | Swarm mode documented in CLAUDE.md but not implemented -- all fields commented out with `TODO: Pending GA` | Medium | `src/lib/agents/stream-handler.ts:21-30`, `CLAUDE.md` | Implement swarm mode or update CLAUDE.md to mark as planned/future |
| 38 | AE-006 | Agent Execution | Fire-and-forget execution with no status guarantee -- `start()` returns before execution begins; returned status may be stale | Medium | `src/services/agent/agent-execution.service.ts:214-254` | Add short initial health check before returning from `start()` |
| 39 | AE-007 | Agent Execution | Module-level `runningAgents` Map not process-safe -- each process has its own map; `stop()` on one process cannot abort agent on another | Medium | `src/services/agent/agent-execution.service.ts:30,449-468` | Store cancellation flag in DB and have stream loop poll; or use stop-file pattern from container service |
| 40 | AE-008 | Agent Execution | `getRunningCount` queries only `running` status, missing `starting` and `planning` -- allows exceeding concurrency limit | Medium | `src/services/agent/agent-execution.service.ts:544-550` | Include `starting` and `planning` in `inArray` filter for running count |
| 41 | AE-010 | Agent Execution | Hooks created but not passed to SDK session -- hooks designed for custom tool model but stream handler uses `tool_use_summary` events; `executeToolWithHooks` never called | Medium | `src/services/agent/agent-execution.service.ts:201-210`, `src/lib/agents/stream-handler.ts:89,657-690` | Integrate hooks into SDK callbacks or remove from planning flow |
| 42 | CS-001 | Container & Sandbox | OAuth token exposed in container environment variables -- visible via `docker inspect` or pod specs | Medium | `src/services/container-agent.service.ts:1239`, `agent-runner/src/index.ts:79` | Use K8s Secrets mounted as file volumes or Docker secrets |
| 43 | CS-002 | Container & Sandbox | In-memory state not synchronized across server restarts -- `runningAgents` Map lost; orphaned container processes continue without tracking | Medium | `src/services/container-agent.service.ts:152-155` | Implement orphan detection on startup; query running containers and re-attach or terminate |
| 44 | CS-006 | Container & Sandbox | K8s env vars passed via shell interpolation with complex branching -- edge cases in shell escaping | Medium | `src/lib/sandbox/providers/agent-sandbox-instance.ts:136-173` | Add integration tests with adversarial values (quotes, backticks, dollar signs, newlines) |
| 45 | CS-007 | Container & Sandbox | No network policy for Docker containers -- agents can make arbitrary network requests | Medium | `docker/docker-compose.yml`, `src/lib/sandbox/providers/docker-provider.ts` | Create dedicated Docker network with restricted egress (allow only Anthropic API) |
| 46 | CS-012 | Container & Sandbox | Plan TTL cleanup may leave orphaned worktrees -- expired plans deleted without cleaning up associated worktrees | Medium | `src/services/container-agent.service.ts:670-688` | Add worktree cleanup to `cleanupExpiredPlans()` before deleting from `pendingPlans` |
| 47 | DB-004 | Database & Data Layer | N+1 query in `ProjectService.listWithSummaries()` -- fetches all projects, then per-project tasks and agents | Medium | `src/services/project.service.ts:183-255` | Replace with single query using `GROUP BY`/`COUNT` aggregation or Drizzle eager loading |
| 48 | DB-005 | Database & Data Layer | No `updatedAt` auto-update mechanism -- every service method manually sets timestamp; error-prone if missed | Medium | `src/services/task.service.ts` (7 assignments), `src/services/project.service.ts`, 3 more files | Create Drizzle middleware or wrapper that auto-sets `updatedAt` on all `update()` calls |
| 49 | DB-007 | Database & Data Layer | Session events offset `MAX(offset)` query per event is a performance bottleneck during high-throughput streaming | Medium | `src/services/durable-streams.service.ts:488-503` | Maintain in-memory offset counter per stream; fall back to DB on restart |
| 50 | DB-008 | Database & Data Layer | Indexes defined in bootstrap SQL not in Drizzle schema -- Drizzle Kit unaware; may not be created in PostgreSQL | Medium | `src/lib/bootstrap/phases/schema.ts:415-427`, `src/db/schema/sqlite/tasks.ts` | Move index definitions into Drizzle schema files using index/constraint callback |
| 51 | DB-012 | Database & Data Layer | Timestamps as text strings prevent SQL date operations -- fragile lexicographic sorting dependency | Medium | `src/db/schema/sqlite/projects.ts:24-25`, `src/services/session/session-crud.service.ts:154-159` | Consider Unix epoch integers for timestamps or document ISO 8601 sorting requirement |
| 52 | DB-013 | Database & Data Layer | Inconsistent timestamp types across tables -- most use `text`, `cli_sessions` uses `integer`, `session_events` uses `integer` | Medium | `src/db/schema/sqlite/cli-sessions.ts:27-28,37-38`, `src/db/schema/sqlite/session-events.ts:19` | Standardize on one timestamp representation; add TypeScript helper functions |
| 53 | TI-001 | Testing Infrastructure | Coverage thresholds (74/64/80/74) below spec targets (80/80/80/80); branch coverage at 64% is concerning | Medium | `vitest.config.ts:46-51` | Incrementally raise thresholds toward 80%; prioritize branch coverage in service layer |
| 54 | TI-003 | Testing Infrastructure | Dual mock systems create confusion -- legacy `services.ts` and newer `mock-services.ts` coexist with "Legacy" suffixes | Medium | `tests/mocks/services.ts:1-369`, `tests/mocks/mock-services.ts:1-505` | Consolidate on `mock-services.ts`; deprecate legacy mocks |
| 55 | TI-004 | Testing Infrastructure | Integration tests do not test real server -- 1500-line file tests local helper functions, not actual Hono server | Medium | `tests/server/api-integration.test.ts:1-1505` | Refactor to test actual server using Hono's `app.request()` method |
| 56 | TI-006 | Testing Infrastructure | E2E tests not in CI pipeline -- only unit tests run in GitHub Actions | Medium | `.github/workflows/ci.yml:44-75` | Add separate CI job for E2E tests that starts dev server and runs `test:e2e` |
| 57 | TI-011 | Testing Infrastructure | No tests for Terraform Compose or Container Agent services -- complex services with multiple failure modes | Medium | `src/services/terraform-compose.service.ts`, `src/services/container-agent.service.ts` | Add unit tests for compose pipeline and container agent lifecycle |
| 58 | PT-001 | Prompts & Templates | Unreplaced template variables silently passed to model -- no validation that required `{{placeholder}}` vars are supplied | Medium | `src/lib/prompts/prompt-service.ts:38-42`, `src/lib/prompts/prompt-registry.ts:524-531` | Add post-substitution check that warns/throws if `{{...}}` patterns remain |
| 59 | PT-004 | Prompts & Templates | Duplicated `mapToCompactNodeType` function in two locations with slightly different implementations | Medium | `src/lib/workflow-dsl/layout.ts:432-462`, `src/app/components/features/workflow-designer/index.tsx:39-63` | Extract to shared utility in `src/lib/workflow-dsl/` |
| 60 | PT-005 | Prompts & Templates | Duplicated HCL/Stacks extraction logic between server and client -- bug fixes must be applied in two places | Medium | `src/services/terraform-compose.service.ts:713-767`, `src/app/components/features/terraform/terraform-context.tsx:100-153` | Extract to shared utilities in `src/lib/terraform/` |
| 61 | PT-006 | Prompts & Templates | Duplicated clarifying question parsers with divergent option inference -- inconsistent UX depending on which runs | Medium | `src/services/terraform-compose.service.ts:844-884`, `src/app/components/features/terraform/terraform-context.tsx:25-97` | Extract to single shared implementation; use client version as canonical |
| 62 | PT-008 | Prompts & Templates | Environment variable filter uses denylist-by-exclusion pattern -- will miss new sensitive variables | Medium | `src/services/terraform-compose.service.ts:401-408` | Invert to allowlist approach; only pass specific env vars the SDK needs |
| 63 | PT-010 | Prompts & Templates | Session memory leak in Compose service -- `cleanupSessions()` only called from `startCompose()`; expired sessions persist between requests | Medium | `src/services/terraform-compose.service.ts:94-122` | Add periodic cleanup via `setInterval` or trigger from `subscribeToJob()` and `getSession()` |
| 64 | CM-001 | CLI Monitor | Hardcoded cost model ignores model-specific pricing -- Sonnet pricing used for all models; Opus is 5x more expensive | Medium | `src/app/components/features/cli-monitor/cli-monitor-utils.ts:30-35` | Look up pricing per model from a model pricing map using `session.model` |
| 65 | CM-004 | CLI Monitor | Missing cost for ephemeral cache tokens -- tokens tracked in totals but not included in cost estimation | Medium | `src/app/components/features/cli-monitor/cli-monitor-utils.ts:24-36` | Add ephemeral cache token pricing or document inclusion in standard cache pricing |
| 66 | CM-005 | CLI Monitor | No authentication on daemon-to-server API -- any local process can register as daemon or inject fake session data | Medium | `packages/cli-monitor/src/agentpane-client.ts:82-88`, `src/server/routes/cli-monitor.ts:248-260` | Implement shared secret exchanged during registration |
| 67 | CM-006 | CLI Monitor | Compaction events array grows unbounded -- no ring buffer cap unlike other arrays; long sessions consume increasing memory | Medium | `packages/cli-monitor/src/parser.ts:453` | Add ring buffer cap (20-30 events) consistent with other arrays |
| 68 | CM-013 | CLI Monitor | DB persistence is fire-and-forget without retry -- failed writes silently lost; historical queries will have gaps | Medium | `src/services/cli-monitor/cli-monitor.service.ts:146-153` | Implement retry queue for failed DB writes or track failure counts as health metric |
| 69 | CC-002 | Cross-Cutting Concerns | No centralized environment variable schema -- 82 raw `process.env.*` accesses across 28 files with no validation | Medium | `src/server/api.ts:22-52`, `src/lib/env.ts`, `src/lib/config/config-service.ts:45-51`, 25 more files | Create centralized `server-env.ts` that validates all env vars at startup using Zod |
| 70 | CC-003 | Cross-Cutting Concerns | Request IDs not propagated to service layer -- generated in middleware but never passed to services or logger | Medium | `src/server/router.ts:57-64`, `src/lib/logging/logger.ts:80` | Implement AsyncLocalStorage-based request context for automatic propagation |
| 71 | CC-004 | Cross-Cutting Concerns | Development auth bypass has no warning or audit trail -- all requests auto-authenticated as `local-dev`; `X-Dev-User` allows impersonation | Medium | `src/lib/api/auth-middleware.ts:108-135` | Log startup warning; add per-request debug logging; require explicit `SKIP_AUTH=true` |
| 72 | CC-009 | Cross-Cutting Concerns | `api.ts` is an oversized god module -- 1419 lines mixing DB init, migrations, service wiring, sandbox init, and shutdown | Medium | `src/server/api.ts` (1419 lines) | Break into `bootstrap/database.ts`, `bootstrap/services.ts`, `bootstrap/sandbox-provider.ts` |
| 73 | CC-010 | Cross-Cutting Concerns | Token/cookie validation fallback accepts tokens without verification -- ANY string accepted as session cookie or Bearer token | Medium | `src/lib/api/auth-middleware.ts:77-81,101-105`, `src/server/router.ts:87` | Pass validator callbacks that check against database; log warning when fallback used |
| 74 | PS-002 | Project Structure | Empty and stale root-level files/directories -- `bob/` (empty), `CONTINUITY.md` (stale), `SPEC_UPDATES.md`, HTML prototypes at root | Low | `bob/`, `CONTINUITY.md:1-11`, `SPEC_UPDATES.md:1-7`, `button-preview.html`, `clawd-buttons.html`, `fix-wireguard-push.sh` | Remove stale items; move prototypes to `specs/wireframes/`; move scripts to `scripts/` |
| 75 | PS-007 | Project Structure | Stale Dependabot grouping patterns reference `*eslint*` and `*prettier*` but project uses Biome | Low | `.github/dependabot.yml:16-19` | Update patterns to reference `@biomejs/*` and `biome` |
| 76 | PS-009 | Project Structure | Irrelevant MCP configuration committed -- `.mcp.json` configures XcodeBuildMCP server, unrelated to this project | Low | `.mcp.json:1-8` | Remove or update `.mcp.json`; consider adding to `.gitignore` |
| 77 | PS-012 | Project Structure | `.DS_Store` files not properly gitignored -- committed before gitignore rule was added | Low | Multiple directories | Remove tracked `.DS_Store` files with `git rm --cached` recursively |
| 78 | PS-014 | Project Structure | Stale worktree in `.worktrees/` from previous task taking up disk space | Low | `.worktrees/create-reusable-terraform-s3-module-with-i0nr5t/` | Add cleanup script or periodic job to prune stale worktrees |
| 79 | BA-005 | Backend API & Services | Inconsistent validation patterns across routes -- three different approaches (parseBody, inline safeParse, manual checking) | Low | `src/server/routes/projects.ts:80-92`, `src/server/routes/agents.ts:57-98`, `src/server/routes/settings.ts:86-98` | Migrate all routes to use `parseBody()` helper; centralize schemas in `validation.ts` |
| 80 | BA-008 | Backend API & Services | Unvalidated query parameters -- `limit`, `offset` parsed with `parseInt()` without bounds checking | Low | `src/server/routes/tasks.ts:27-28`, `src/server/routes/sessions.ts:125-126`, 2 more files | Create Zod schemas for query params; clamp `limit` to 1-200; ensure `offset >= 0` |
| 81 | BA-010 | Backend API & Services | Logging inconsistency -- 174 raw `console.*` calls in route files vs. structured `createLogger` in infrastructure | Low | All route files (174 occurrences) | Replace with structured logger passing request ID from middleware context |
| 82 | BA-011 | Backend API & Services | `InMemoryDurableStreamsServer` (120 lines) embedded inline in `api.ts` with infinite polling loop resource leak | Low | `src/server/api.ts:364-483` | Extract to own file; add cancellation mechanism to `subscribe()` generator |
| 83 | BA-012 | Backend API & Services | SSE endpoint manually sets CORS headers instead of relying on Hono CORS middleware; hardcoded to `localhost:3000` | Low | `src/server/routes/sessions.ts:550-558`, `src/server/shared.ts:6-10` | Let Hono CORS middleware handle CORS; use `c.header()` if raw Response needed |
| 84 | BA-015 | Backend API & Services | Unused `SSETokenService` -- 431-line class with full token lifecycle but not imported anywhere | Low | `src/server/sse-token.service.ts:1-431` | Integrate into SSE endpoints or remove dead code |
| 85 | FE-007 | Frontend Architecture | Connection state set to `'connected'` before EventSource actually opens | Low | `src/app/hooks/use-agent-stream.ts:102`, `src/app/hooks/use-session.ts:209` | Remove immediate `setConnectionState('connected')`; rely on `onopen` handler |
| 86 | FE-008 | Frontend Architecture | DurableStreamsClient has 1100+ lines with repetitive switch cases -- 18 near-identical parsing and routing cases | Low | `src/lib/streams/client.ts` (1106 lines) | Create registry/map pattern to reduce by ~400 lines |
| 87 | FE-009 | Frontend Architecture | Duplicate theme application logic in three separate files | Low | `src/app/components/features/theme-toggle.tsx:35-47`, `src/app/routes/settings/appearance.tsx:294`, `src/app/components/features/global-settings.tsx:337` | Extract to shared `useTheme` hook or utility |
| 88 | FE-010 | Frontend Architecture | Custom `cn` utility does not resolve Tailwind class conflicts -- no `tailwind-merge` | Low | `src/lib/utils/cn.ts:24-26` | Add `tailwind-merge` if class conflicts become practical issue |
| 89 | FE-011 | Frontend Architecture | TerraformContext uses separate SSE implementation instead of shared DurableStreamsClient | Low | `src/app/components/features/terraform/terraform-context.tsx:292-465` | Abstract SSE line parsing into shared utility |
| 90 | FE-015 | Frontend Architecture | Search input on dashboard missing `aria-label` for screen reader users | Low | `src/app/routes/index.tsx:467-472` | Add `aria-label="Search projects"` |
| 91 | AE-011 | Agent Execution | Accumulated text overwrite in assistant message handler -- only retains last message's text in multi-turn | Low | `src/lib/agents/stream-handler.ts:171-173,447-449` | Document behavior with comment; accumulate across turns if full history needed |
| 92 | AE-012 | Agent Execution | Full `process.env` passed to SDK session -- exposes all server env vars including DB URLs and API keys to agent subprocess | Low | `src/lib/agents/stream-handler.ts:124,395`, `src/lib/agents/agent-sdk-utils.ts:70` | Create filtered env object with only needed variables (ANTHROPIC_API_KEY, PATH, HOME) |
| 93 | AE-013 | Agent Execution | Tool whitelist hook allows all tools when list is empty -- could be surprising default | Low | `src/lib/agents/hooks/tool-whitelist.ts:7-9` | Add documenting comment; consider default set of safe tools |
| 94 | AE-014 | Agent Execution | Streaming hook tool call pairing can fail for identical concurrent calls due to Map key collision | Low | `src/lib/agents/hooks/streaming.ts:40-58,67` | Use composite key with sequence counter or SDK's `tool_use_id` |
| 95 | AE-015 | Agent Execution | Duplicate task move to `in_progress` -- two DB writes for same column transition with intermediate orphan state | Low | `src/services/agent/agent-execution.service.ts:106,129-140` | Combine into single atomic update |
| 96 | AE-016 | Agent Execution | `currentTurn` hardcoded to 0 in error handler context -- turn-limit check in `handleAgentError` will never trigger | Low | `src/services/agent/agent-execution.service.ts:408-413` | Accept turn count as part of error propagation |
| 97 | CS-003 | Container & Sandbox | TOCTOU race between sandbox validation and exec -- small window where container could stop between check and exec | Low | `src/services/container-agent.service.ts:1219-1232` | Ensure Docker sandbox implements `refreshStatus` for defense-in-depth |
| 98 | CS-004 | Container & Sandbox | Agent-runner tool registry unused -- 6 custom tool definitions never called in current flow; dead code | Low | `agent-runner/src/tools/index.ts`, `agent-runner/src/tools/bash-tool.ts`, 2 more files | Remove unused tool registry or document future intent |
| 99 | CS-005 | Container & Sandbox | Credentials file verification reads back file -- brief memory exposure window with parsed token | Low | `agent-runner/src/index.ts:231-241` | Low risk; consider removing verification read in production builds |
| 100 | CS-008 | Container & Sandbox | Hardcoded default sandbox image in two locations -- can drift if one updated without other | Low | `src/lib/sandbox/types.ts:117`, `src/lib/sandbox/controllers/sandbox-controller.ts:30` | Import `SANDBOX_DEFAULTS.image` from `types.ts` in controller |
| 101 | CS-009 | Container & Sandbox | Warm pool uses `Math.random()` for naming -- non-cryptographic RNG for resource naming | Low | `src/lib/sandbox/controllers/sandbox-controller.ts:782-788` | Use `crypto.randomUUID()` or cuid2 for consistency |
| 102 | CS-010 | Container & Sandbox | Status patching uses GET+PUT instead of PATCH -- read-modify-write race condition | Low | `src/lib/sandbox/controllers/sandbox-controller.ts:653-681` | Use K8s `patchNamespacedCustomObjectStatus` with merge-patch |
| 103 | CS-011 | Container & Sandbox | Docker Compose uses hardcoded Postgres credentials (dev only) | Low | `docker/docker-compose.postgres.yml:10-12` | Add prominent comment noting dev-only; ensure never used in production |
| 104 | CS-013 | Container & Sandbox | Agent-runner logs token presence to stderr -- confirms token use in logs | Low | `agent-runner/src/index.ts:203-204` | Remove log line or gate behind debug-only flag |
| 105 | CS-014 | Container & Sandbox | Container bridge does not validate event data schema -- malicious agent-runner could emit unexpected data shapes | Low | `src/lib/agents/container-bridge.ts:138-188` | Add per-event-type schema validation before publishing |
| 106 | CS-015 | Container & Sandbox | Production Dockerfile copies full `src/` directory into runtime image -- increases size and attack surface | Low | `docker/Dockerfile:45` | Only copy `dist/` if compiled; use `.dockerignore` for test/dev files |
| 107 | CS-016 | Container & Sandbox | Sudo access in agent sandbox for chown -- agent could change ownership of sensitive files | Low | `docker/Dockerfile.agent-sandbox:64` | Document security rationale; ensure entrypoint only uses for `/workspace` |
| 108 | DB-006 | Database & Data Layer | Legacy alias `sqlite as pglite` is misleading -- comment warns it's SQLite, not PostgreSQL | Low | `src/db/client.ts:125-127` | Remove the `pglite` alias; verify no imports reference it |
| 109 | DB-009 | Database & Data Layer | Missing foreign key on `terraform_modules.registry_id` -- Drizzle relation exists but DB-level constraint missing | Low | `src/db/schema/sqlite/terraform.ts:43`, `src/db/schema/sqlite/relations.ts:213-218` | Add `.references(() => terraformRegistries.id, { onDelete: 'cascade' })` |
| 110 | DB-010 | Database & Data Layer | Schema test coverage is minimal -- only verifies table objects and `id` columns exist | Low | `src/db/schema/__tests__/schema.test.ts:1-59` | Add roundtrip, JSON serialization, FK constraint, default value, and uniqueness tests |
| 111 | DB-011 | Database & Data Layer | No soft delete strategy -- hard deletes with cascade destroy audit history when parent records deleted | Low | `src/services/task.service.ts:308-319`, `src/services/project.service.ts:285-307` | Consider soft deletes for audit tables; use `ON DELETE SET NULL` for `audit_logs` and `agent_runs` |
| 112 | TI-007 | Testing Infrastructure | Playwright installed but unused -- devDependency with no config file; E2E tests use `agent-browser` CLI wrapper | Low | `package.json:46,59` | Adopt Playwright directly or remove unused dependency |
| 113 | TI-008 | Testing Infrastructure | E2E tests use `expect(true).toBe(true)` as fallback -- tests always pass regardless of functionality | Low | `tests/e2e/kanban-workflow.test.ts:73-74,109,139,174` | Use `it.skipIf(!serverRunning)` instead of try/catch with unconditional pass |
| 114 | TI-009 | Testing Infrastructure | jsdom environment used for all tests including pure backend -- adds unnecessary DOM overhead | Low | `vitest.config.ts:7` | Change default to `node`; annotate component files with `// @vitest-environment jsdom` |
| 115 | TI-010 | Testing Infrastructure | Ad-hoc `createDbMock()` in service tests instead of using centralized `createMockDatabase()` | Low | `src/services/__tests__/task.service.test.ts:6-14`, 2 more files | Migrate to `createMockDatabase()` from `tests/mocks/mock-builders.ts` |
| 116 | TI-012 | Testing Infrastructure | Test specification divergence -- actual Vitest config differs from spec in environment, pool, thresholds, and includes | Low | `specs/application/testing/test-infrastructure.md:1-80`, `vitest.config.ts:1-65` | Align spec to implementation or vice versa; keep one source of truth |
| 117 | PT-002 | Prompts & Templates | Dynamic variables declaration is informational only -- no compile-time or runtime enforcement | Low | `src/lib/prompts/prompt-registry.ts:469`, `src/lib/prompts/types.ts:25` | Add startup validation scanning `defaultText` for `{{...}}` patterns; verify against declared list |
| 118 | PT-003 | Prompts & Templates | Large monolithic prompt registry file -- all 8 prompts in 591-line single file | Low | `src/lib/prompts/prompt-registry.ts:1-591` | Split into per-category files; import into registry |
| 119 | PT-007 | Prompts & Templates | Workflow merge prompt not in registry -- hardcoded inline, cannot be customized via settings | Low | `src/lib/workflow-dsl/ai-prompts.ts:174-202` | Register `'workflow-merge'` prompt in registry |
| 120 | PT-009 | Prompts & Templates | No reactive template updates in workflow designer -- fetches once on mount; stale until page reload | Low | `src/app/components/features/workflow-designer/index.tsx:244-259` | Add refresh button or polling to periodically refresh template list |
| 121 | PT-011 | Prompts & Templates | `fullResponse` accumulation race condition risk -- edge case with empty stream deltas followed by assistant message | Low | `src/services/terraform-compose.service.ts:418-478` | Add secondary guard based on `fullResponse.length > 0` |
| 122 | PT-012 | Prompts & Templates | Workflow AI response parsing silently drops invalid nodes -- user gets no indication of dropped content | Low | `src/server/routes/workflow-designer.ts:161-168,174-179` | Include dropped node/edge count in API response; reduce `aiConfidence` proportionally |
| 123 | PT-013 | Prompts & Templates | ELK layout dimension mismatch between server and client -- server layout computation effectively wasted | Low | `src/server/routes/workflow-designer.ts:557-564`, `src/lib/workflow-dsl/layout.ts:53-61` | Define shared layout constants in `src/lib/workflow-dsl/layout.ts` |
| 124 | PT-014 | Prompts & Templates | Hardcoded user initials "SL" in Terraform chat panel | Low | `src/app/components/features/terraform/terraform-chat-panel.tsx:698` | Derive from authenticated user's name or use generic icon |
| 125 | PT-015 | Prompts & Templates | Conditional edge type reuses `SequentialEdge` component without visual differentiation | Low | `src/app/components/features/workflow-designer/edges/index.ts:35` | Implement dedicated `ConditionalEdge` or document visual identity by design |
| 126 | CM-002 | CLI Monitor | PID lock file has race condition -- non-atomic check-then-write; small window for concurrent daemon starts | Low | `packages/cli-monitor/src/daemon.ts:29-46` | Use atomic `O_EXCL` file creation or accept low risk for single-user usage |
| 127 | CM-003 | CLI Monitor | Incorrect repository URLs in `package.json` -- points to `agentdevsl/claudorc` instead of actual repo | Low | `packages/cli-monitor/package.json:21-28` | Update URLs to match actual monorepo location |
| 128 | CM-007 | CLI Monitor | Sequential file scanning at startup -- processes all JSONL files one at a time | Low | `packages/cli-monitor/src/watcher.ts:109-118` | Use parallel processing with concurrency limit for initial scan |
| 129 | CM-008 | CLI Monitor | `realpath` called on every file process -- watch directory resolved repeatedly instead of once | Low | `packages/cli-monitor/src/watcher.ts:140-153` | Cache resolved watch directory path in `FileWatcher.start()` |
| 130 | CM-009 | CLI Monitor | Agent type derivation uses fragile string matching -- `includes('cod')` matches "decoder" too | Low | `packages/cli-monitor/src/parser.ts:518-570` | Use word boundary patterns or prioritize `subagentType` field from JSONL |
| 131 | CM-010 | CLI Monitor | `getContextWindowLimit()` returns same value for all models | Low | `packages/cli-monitor/src/parser.ts:92-98` | No immediate action; consider making configurable |
| 132 | CM-011 | CLI Monitor | Heartbeat `stale` vs `unknown` distinction lost -- both return 409; client treats identically | Low | `src/services/cli-monitor/cli-monitor.service.ts:65-74`, `packages/cli-monitor/src/agentpane-client.ts:106-111` | Remove distinction or propagate with different status codes |
| 133 | CM-014 | CLI Monitor | Server-side topology BFS uses linear search `sessions.find()` instead of Map lookup | Low | `src/server/routes/cli-monitor.ts:465` | Build `Map<sessionId, session>` before BFS loop |
| 134 | CM-015 | CLI Monitor | No validation of `--retention` flag -- accepts 0, negative, or extremely large values | Low | `packages/cli-monitor/src/index.ts:38-39` | Clamp to reasonable range (1-365 days); warn on out-of-range |
| 135 | CM-016 | CLI Monitor | CLI argument parser does not handle short flags or `=` syntax | Low | `packages/cli-monitor/src/index.ts:12-24` | Use Node.js built-in `parseArgs` from `node:util` |
| 136 | CM-017 | CLI Monitor | `touchSessionsByFilePath` iterates all sessions -- no reverse index from file path to session IDs | Low | `packages/cli-monitor/src/session-store.ts:248-256` | Add `Map<filePath, Set<sessionId>>` index if perf becomes issue |
| 137 | CM-019 | CLI Monitor | No rate limiting on SSE connections per client -- single client can consume all 50 global connections | Low | `src/server/routes/cli-monitor.ts:363-371` | Add per-IP or per-user connection limits |
| 138 | CM-020 | CLI Monitor | Shallow copy in `flushChanges()` -- nested objects are shared references; mutations can leak | Low | `packages/cli-monitor/src/session-store.ts:209` | Use `structuredClone()` or accept risk given 500ms window |
| 139 | CC-005 | Cross-Cutting Concerns | Settings service stores JSON without schema validation -- corrupt settings cause runtime crashes | Low | `src/server/api.ts:644,675,714,1263` | Create Zod schemas for each settings key; validate on read |
| 140 | CC-006 | Cross-Cutting Concerns | Inconsistent error handling in migration code -- 5 repeated try/catch blocks checking for duplicate column by string matching | Low | `src/server/api.ts:175-236` | Extract `runIdempotentMigration()` helper; migrate to Drizzle Kit tracking |
| 141 | CC-007 | Cross-Cutting Concerns | Rate limiter is in-memory only -- does not work across multiple instances | Low | `src/lib/api/rate-limiter.ts:32` | Implement Redis-backed limiter when scaling horizontally; acceptable for current single-instance |
| 142 | CC-008 | Cross-Cutting Concerns | Encryption key file created with default permissions -- may be readable by other system users | Low | `src/server/crypto.ts:34` | Set restrictive file permissions `mode: 0o600` when writing keyfile |
| 143 | CC-011 | Cross-Cutting Concerns | Duplicate `createError` + `AppErrorClass` patterns -- class-based version defined but unused | Low | `src/lib/errors/base.ts:8-31` | Remove `AppErrorClass` if unused; document if planned for future use |
| 144 | FE-012 | Frontend Architecture | Dashboard polling at 15s idle may be excessive; no visibility-based pause | Info | `src/app/routes/index.tsx:239` | Use `visibilitychange` to pause polling when tab not visible; increase idle interval |
| 145 | FE-013 | Frontend Architecture | Kanban board multi-select without batch move -- cards highlighted but not moved together | Info | `src/app/components/features/kanban-board/index.tsx:158-194` | Implement batch move or remove multi-select affordance |
| 146 | FE-014 | Frontend Architecture | `useSession` join race condition -- fire-and-forget `join()` before SSE subscription may miss events | Info | `src/app/hooks/use-session.ts:123` | Await join before subscribing or accept race condition |
| 147 | CM-012 | CLI Monitor | `ingestTimer.unref` guard unnecessary for stated `node >= 22.0.0` engine requirement | Info | `packages/cli-monitor/src/daemon.ts:213,243` | Remove guards for cleaner code |
| 148 | CM-018 | CLI Monitor | `--retention` flag not passed when value is 0 due to `if (options.retentionDays)` falsy check | Info | `packages/cli-monitor/src/daemon.ts:68-70` | Change guard to `if (options.retentionDays != null)` |
