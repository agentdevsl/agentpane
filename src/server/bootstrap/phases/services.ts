/**
 * Service Construction Phase
 *
 * Delegates to the ServiceContainer factory.
 * This thin phase wrapper exists for consistency with other bootstrap phases.
 */

export { createServiceContainer } from '../service-container.js';
